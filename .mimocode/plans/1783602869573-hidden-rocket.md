# Plano: Atualizar tests/admin.test.ts

## Arquivo a modificar

`tests/admin.test.ts`

## Mudanças

Reescrever seguindo a lógica fornecida pelo usuário:

1. **Emails**: usar `admin@sprint6.com` e `cliente@sprint6.com` em vez de `@test.com`
2. **beforeAll**: desestruturar `adminBody` e `custBody` (mesmo `adminBody` não sendo usado diretamente — mantém consistência com o código do usuário)
3. **Dashboard**: assertions mais simples — verificar `overview`, `recentOrders`, `topProducts`, `ordersByStatus`
4. **Cupons**: assertions mais simples — criar, duplicado 409, listar, atualizar, remover
5. **Estoque**: assertions mais simples — alerts, movements, 403 para customer
6. **Remover testes extras** que o usuário não incluiu (401 sem token, paginação customizada, 422 dados inválidos, 404 inexistente)
7. **Manter**: `import app from '../src/app'` (default export), `cleanDatabase`, `prisma.$disconnect`
